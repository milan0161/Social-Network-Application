import React from 'react';
// import { useGetFollowersQuery } from '../features/user/api/userApiSlice';
import NewFeedsList from '../features/post/data/NewFeedsList';

const Home: React.FunctionComponent = () => {
  return <NewFeedsList />;
};

export default Home;
